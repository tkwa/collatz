name: Lean (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the pinned Lean project in Docker
        run: docker build --progress=plain --file lean/Dockerfile --tag collatzbench-lean .
